#include <iostream>

int main() {
  // This is a comment
  // by using // at the beginning of each line and instead of using /*
  // or */ to delimit comments
  std::cout << "Does this work?" << std::endl;
  return 0;
}
