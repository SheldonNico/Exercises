// using std=c++11 flag
#include <map>
#include <vector>
#include <algorithm>
#include <string>

using std::pair;
std::map<std::string, pair<double, double>> levels = {{"A", {90, 100} }, {"B", {80, 89.99}}};

int main() {
  return 0;
}
