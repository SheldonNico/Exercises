fn main() {
    // assert_eq!(leetcode::topic2_graph::p1631_minimum_effort_path([[1,2,2],[3,8,2],[5,3,5]].iter().map(|s| s.into_iter().map(|s| *s as i32).collect()).collect()), 4);
    // use leetcode::advcode2020::*;
    // p20()

    leetcode::advcode2020::p24();
}
